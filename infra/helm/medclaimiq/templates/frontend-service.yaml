apiVersion: v1
kind: Service
metadata: {name: medclaimiq-frontend, labels: {app.kubernetes.io/name: medclaimiq, app.kubernetes.io/component: frontend}}
spec:
  selector: {app.kubernetes.io/name: medclaimiq, app.kubernetes.io/component: frontend}
  ports: [{name: http, port: 3000, targetPort: http}]
